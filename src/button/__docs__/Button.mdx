import { ArgTypes, Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Button from "./Button.stories.tsx";

<Meta of={Button} title="Button" />

# Button

Button component that provides the user with a way to perform an action.

## Variants

### Primary Button

<Canvas of={Button.Primary} />

### Secondary Button

<Canvas of={Button.Secondary} />

### Ghost Button

<Canvas of={Button.Ghost} />

## Usage

```ts
import {Button} from "caffeine-components";

const Example = () => {
  return (
      <Button
        disabled={false}
        label={"Button"}
        onClick={()=> console.log("Clicked")}
        type={"primary"}
      />
  );
};

export default Example;
```

## Props

<ArgTypes of={Example} />
